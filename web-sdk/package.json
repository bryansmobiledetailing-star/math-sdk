{
  "name": "web-sdk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ways dev",
    "build": "pnpm --filter ways build",
    "storybook": "pnpm --filter ways storybook"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
